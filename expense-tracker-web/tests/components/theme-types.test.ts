import { describe, it, expect } from 'vitest'

import * as Module from '../../src/components/theme-types'

describe('theme-types.ts', () => {
  it('should have tests', () => {
    expect(true).toBe(true)
  })
})
