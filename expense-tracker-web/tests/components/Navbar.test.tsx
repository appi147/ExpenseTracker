import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import * as Module from '../../src/components/Navbar'

describe('Navbar.tsx', () => {
  it('should have tests', () => {
    expect(true).toBe(true)
  })
})
