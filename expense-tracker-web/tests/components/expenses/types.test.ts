import { describe, it, expect } from 'vitest'

import * as Module from '../../../src/components/expenses/types'

describe('types.ts', () => {
  it('should have tests', () => {
    expect(true).toBe(true)
  })
})
