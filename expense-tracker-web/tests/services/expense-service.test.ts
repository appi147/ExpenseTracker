import { describe, it, expect } from 'vitest'

import * as Module from '../../src/services/expense-service'

describe('expense-service.ts', () => {
  it('should have tests', () => {
    expect(true).toBe(true)
  })
})
