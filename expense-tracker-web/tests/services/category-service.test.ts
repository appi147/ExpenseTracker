import { describe, it, expect } from 'vitest'

import * as Module from '../../src/services/category-service'

describe('category-service.ts', () => {
  it('should have tests', () => {
    expect(true).toBe(true)
  })
})
