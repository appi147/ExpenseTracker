import { describe, it, expect } from 'vitest'

import * as Module from '../../src/services/sub-category-service'

describe('sub-category-service.ts', () => {
  it('should have tests', () => {
    expect(true).toBe(true)
  })
})
